{
  "name": "Elastic-Beanstalk-Sample-App",
  "version": "0.0.1",
  "private": true,
  "dependencies": {
    "express": "^4.17.2"
  },
  "scripts": {
    "start": "node app.js",
    "build": "cd client && yarn build",
    "postbuild": "cd client && cp -R build/* ../public",
    "mover": "cd client && cp -R build/* ../public"
  }
}
